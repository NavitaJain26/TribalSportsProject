<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techisquad.tribalsports">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".verify_trib_activity" />
        <activity android:name=".update_trib_activity" />
        <activity android:name=".events_details_activity" />
        <activity android:name=".events_input_activity" />
        <activity android:name=".eligibility_pdf_activity" />
        <activity android:name=".navigation_bar_activity"></activity>
    </application>

</manifest>